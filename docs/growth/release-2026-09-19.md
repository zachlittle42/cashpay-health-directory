# Growth release verification — September 19, 2026

The ED growth sprint is live at https://vitalityscout.com. Content shipped in `b1e6e6b`; the verified analytics correction shipped in `81e9494`. Both production deployments reached Ready. The final application deployment is `dpl_BWvE7WaWvTADSxenLmokMHKzpu36`, with the live custom domain assigned.

## Measurement cutover

- Google Analytics/GTM collection is intentionally paused in this release. No Google or Meta script was present in the consented production browser check. The owner selected PostHog after the available Google reporting account could not edit automatic measurement settings.
- PostHog schema-v2 delivery was verified at **2026-09-19 19:53:42.943 UTC**. Use **2026-09-19 19:54:00 UTC** as a conservative not-before boundary for the new funnel; the first complete UTC reporting day starts **September 20**.
- Start a fresh consented baseline. Do not compare raw counts or rates directly with the September 17 baseline, which predates consent changes. GSC and Bing remain independent acquisition sources.
- Short post-release windows are partial observations, not a complete weekly comparison. Unknown operational contacts, qualified leads, bookings and revenue remain unavailable.

## Actual delivery evidence

A browser visit used the explicit campaign `utm_source=internal&utm_medium=qa&utm_campaign=growth_sprint_qa`. After allowing analytics, one Hims provider button was activated. The aggregate PostHog query was restricted to `vitality-web`, schema version 2, this QA campaign and timestamps after 19:50 UTC.

| Observed event | Count | Public path | Provider / destination |
| --- | ---: | --- | --- |
| `$pageview` | 1 | `/guides/online-ed-treatment` | — |
| `provider_click` | 1 | `/guides/online-ed-treatment` | `hims` / `www.hims.com` |

The browser ingestion request returned HTTP 200. Neither received event contained the checked contact fields (`email`, `name`, `message`, `condition`) or a query/fragment in `$current_url`. No form was submitted, no contact notification was sent, and no clinical intake or purchase was performed. The browser was returned to Necessary only and the guide was opened without QA parameters afterward. Scheduled reporting excludes QA campaigns.

The initial `b1e6e6b` browser check exposed HTTP 401 ingestion failures: replacing PostHog's properties object had removed its required public project token. `81e9494` preserves the configured token while continuing to discard private fields. A regression test now covers this SDK transport requirement. The first deployment is therefore not the verified funnel cutover.

## Search and page checks

At 19:44:45 UTC, all six priority pages returned HTTP 200, self-referencing canonicals and no robots noindex directive. All were present in the live sitemap:

- `/guides/online-ed-treatment`
- `/guides/bluechew-cost`
- `/guides/sildenafil-cost-without-insurance`
- `/mens-health`
- `/guides/mri-cost-without-insurance`
- `/editorial-policy`

The live ED URL and original H1 were retained. Desktop and 390px mobile checks verified readable comparisons without page overflow, expandable terms, correct provider destinations and working privacy preferences. The MRI guide has three explicit provider handoffs. Unconfigured inquiry/email components display useful public links and no contact inputs.

IndexNow accepted these six paths plus `/`, `/guides` and `/telehealth` at **2026-09-19 19:46:22.303 UTC**, HTTP 200, using the site's existing public ownership file. This verifies receipt, not indexing. Google inclusion remains a monitored outcome; no Google indexing success is claimed. See the [IndexNow response definition](https://www.indexnow.org/documentation).

## Validation and activation dependencies

All **19** focused regression tests pass; TypeScript, lint and the full **930-page** production build pass. Existing unrelated anonymous-default-export warnings in provider data remain. Both content and analytics corrections were deployed and checked on the live domain.

The existing daily/weekly reporting prompts were migrated and verified on the worker; see the [schedule migration record](schedule-migration-2026-09-17.md). Their next completed report is a separate execution outcome, not established by prompt delivery alone.

Automatic inquiry delivery still requires a verified Resend sender/account and an explicit operational recipient. Paid referral attribution requires approved partner URLs and outcome reporting. Direct provider links work now. The [partner launch packet](../referral-partner-launch.md) and [measurement contract](../growth-measurement.md) contain activation steps. No partnership, delivered inquiry, booking or revenue is implied by this release.
